#include "pch.h"

#include <algorithm>
#include <string>
#include <utility>
#include <iostream>
#include <vector>
#include <random>

using namespace std;

vector<vector<int>> store;
vector<vector<int>> response;
vector<int> broken;


/*mt19937 eng; // This is the Mersenne Twister
uniform_int_distribution<int> dist(-1000, 1000);

for (int j = 0; j < 10; j++)
{
    for (int i = 0; i < 10000; i++)
        cout << dist(eng) << " ";
    cout << endl;
}
return 0;*/

void run(int tn)
{
    int N, B, F;
    cin >> N >> B >> F;
}

int main3()
{
    int testcases; cin >> testcases;

    for (int testcase = 1; testcase <= testcases; testcase++)
        run(testcase);

    return 0;
}
